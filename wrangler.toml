name = "my-vless-proxy"  # 随意改成你的Worker名称（英文+数字+连字符，不能有空格）
main = "Vless_workers_pages/_worker.js"  # 入口文件路径（仓库最新结构就是这个）
compatibility_date = "2025-12-19"  # 当前日期，保持最新

workers_dev = true  # 启用workers.dev子域名